import java.time.Month;

public class Menus
{

    public static void EVTCreationMenu()
    {

    }

    public static int EVTEditMenu()
    {/*
        System.out.print("What event would you like to edit?");
        //add how to get the input and show how to get the events possible to appear
        System.out.print("What would you like to edit about the event?");
        // add input getter for either month, day, year, hour, or minute
        int input;
        if (getting input  == Month)
            input = 1;
        if (getting input  == Day)
            input = 2;
        if (getting input  == Year)
            input = 3;
        if (getting input  == Hour)
            input = 4;
        if (getting input  == Minute)
            input = 5;
        while (menu.is.open() == true)
        {
        switch (input)
        {
            case 1:
            {
                DateTime.ChangeMonth();
                break;
            }
            case 2:
            {
                DateTime.ChangeDay();
                break;
            }
            case 3:
            {
                DateTime.ChangeYear();
                break;
            }
            case 4:
            {
                DateTime.ChangeHour();
                break;
            }
            case 5:
            {
                DateTime.ChangeMinute();
                break;
            }
            }
        }*/
        return 0;
    }
}
